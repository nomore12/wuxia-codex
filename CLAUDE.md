# CLAUDE.md

무협 세계관 구축 저장소. 모든 응답과 문서는 **한국어**로 작성한다.

---

## 절대 규칙

1. **`canon/` 아래 파일을 수정하지 않는다.**
   수정이 필요해 보이면 고치지 말고 **보고한다.**
   내용에 모순이나 오류를 발견해도 마찬가지다.

2. **새로 만드는 문서는 전부 `drafts/`에 둔다.**
   정전 승격은 작성자만 한다. 절대 자동화하지 않는다.

3. **`미확정사항.md`의 항목을 채우지 않는다.**
   이것은 빈칸이 아니라 **잠금**이다.
   해당 항목을 참조하는 서술이나 데이터도 생성하지 않는다.

4. **추측으로 채우지 않는다.** 판단이 필요하면 멈추고 묻는다.

---

## 작업 전에 읽을 것

정전은 절 단위로 나뉘어 있다. **전부 읽지 말고 필요한 절만 연다.**

| 작업            | 읽을 것                                                                  |
| --------------- | ------------------------------------------------------------------------ |
| 무엇이든        | `canon/body-and-mind/00_INDEX.md` — 항상. 어느 절이 필요한지 여기서 판단 |
| 1차 (이름 나열) | `specs/1차작업규칙.md` + `specs/세력별_차별화지시.md`                    |
| 세력 문서 작성  | 위 + `canon/세력목록.md` + `canon/무공색인.md`                           |
| 심법 설계       | `canon/body-and-mind/04_마음.md`                                         |
| 무공 설계       | `canon/body-and-mind/05_힘.md` `06_무공과심법.md`                        |
| 인물 배치       | `canon/body-and-mind/01_두개의축.md` `02_관문과문턱.md` `10_스키마.md`   |
| 주화입마 판정   | `canon/body-and-mind/07_주화입마.md` (+ `02` `04` `05`)                  |
| 강함·평판 서술  | `canon/body-and-mind/08_별호와평판.md`                                   |

각 절 파일의 frontmatter에 `requires`가 있다. **그 절들도 함께 읽는다.**

생성 작업 전에는 `specs/AI생성규칙.md`를 읽는다.

---

## 세계관 금지 사항

자주 어기게 되는 것들이다. 위반하면 결과물을 폐기해야 한다.

### 기(氣)를 실체로 쓰지 않는다

내공은 몸 안을 흐르는 에너지가 **아니다.** 마음을 힘으로 바꾼 것이다.

- **명칭에 氣·眞氣·內力·罡氣를 쓰지 않는다.** 관용 명칭이라도 예외 없다.
- 경맥·혈도·단전은 **기의 통로가 아니라 힘이 지날 때 몸이 견디는 골조**다.
- **내공이 소모·회복된다고 쓰지 않는다.** 소모되는 것은 몸이다.
- **흡성대법 계통을 만들지 않는다.** 남의 마음은 가져올 수 없다.

### 등급표를 만들지 않는다

- 삼류·이류·일류·절정·초절정·화경·현경 같은 체계는 **존재하지 않는다.**
- **"절정이면 검기, 초절정이면 검강" 같은 대응표를 만들지 않는다.**
  깊이는 무엇을 할 수 있는지 결정하지 않는다.
- 같은 현상을 문파마다 다르게 부른다. 통합 명칭은 없다.

### 수치를 계산하지 않는다

심/체 depth는 구간 표시이지 전투력이 아니다.

- 더하거나 평균 내지 않는다
- 문파의 평균 깊이를 구하지 않는다
- 임계값을 스스로 만들지 않는다 (`gap >= 3`, `fit >= 0.8` 등)
- `fit`에 임의의 값을 넣지 않는다. `null`로 둔다

### 주화입마를 남발하지 않는다

- **다룰 수 있는 것은 2층까지다.** 3층(역행)은 작성자가 배치한다
- 감정을 느끼는 것 자체는 어긋남이 아니다
- 행동의 격렬함은 층의 근거가 아니다
- **심법을 참조하지 않고 판정하지 않는다.** 보편 기준은 없다

### 깊이 8 이상을 함부로 만들지 않는다

- 8–9는 **작성자 승인 후에만** 추가한다
- 10은 **자율 생성 금지**
- 서사적 위기를 "숨은 고수"로 해결하지 않는다

---

## 명명 규칙

| 대상             | 표기 |
| ---------------- | ---- |
| 폴더명           | 영문 |
| 사람이 읽는 문서 | 한글 |
| 스크립트         | 영문 |
| `sect_id`        | 영문 |

세력 문서는 `drafts/factions/{한글 세력명}.md`,
frontmatter의 `sect_id`는 영문(`sorim`, `dangga`, `cheonma`).
**파일명과 `sect_id`가 달라도 된다.** validate.py가 `sect_id`를 읽는다.

---

## 생성 작업 절차

1. `canon/무공색인.md`를 **먼저 조회**한다. 중복을 피하기 위함이다
2. `specs/세력별_차별화지시.md`에서 해당 세력의 지시를 확인한다
   — 카테고리 구성, 규모, 서술 형식이 세력마다 다르다
3. 생성한다
4. `python scripts/validate.py <파일>` 을 실행한다
5. 통과하면 색인에 신규 항목을 추가한다
6. **작업 보고를 남긴다** — 항목 수, origin 비율, 색인 충돌, 판단이 필요한 것

### 서술 형식을 통일하지 않는다

세력마다 문서의 **생김새 자체가 달라야 한다.**
표 구성, 항목 수, 서술 톤이 같으면 40개가 전부 같은 문파처럼 읽힌다.
`specs/세력별_차별화지시.md`에 배정된 형식을 반드시 따른다.

---

## 멈추고 물어야 할 때

- 관용 명칭에 금지 한자가 있는데 마땅한 대체가 없을 때
- 다른 세력과 명칭이 겹치는데 어느 쪽에 둘지 불분명할 때
- 차별화 지시가 그 세력에 맞지 않아 보일 때
- 창작 비율이 예상치와 20%p 이상 어긋날 때
- 정전에서 모순을 발견했을 때
- 미확정 항목을 참조해야만 작업이 진행될 때

**비워두고 묻는 것이 추측으로 채우는 것보다 낫다.**

---

## Git

- 커밋 메시지에 **공동저자(Co-Authored-By) 표기를 넣지 않는다.**
- 생성된 도구 서명(`Generated with...`)도 넣지 않는다.
- **validate.py를 통과한 뒤에만 커밋한다.**
- 커밋 단위는 세력 하나. 메시지는 `1차: {세력명}` 형식.
- `canon/` 변경이 포함된 커밋은 만들지 않는다.
