# 프로젝트 지침

무협 세계관 구축 저장소. 모든 응답과 문서는 **한국어**로 작성한다.

---

## 절대 규칙

1. **`canon/` 아래 파일을 수정하지 않는다.**
   작성자가 명시적으로 지시한 경우에만 고친다.
   내용에 모순이나 오류를 발견해도 **고치지 말고 보고한다.**

2. **지시 범위를 넘지 않는다.**
   지시에 없는 파일을 고치지 않는다. 지시에 없는 항목을 고치지 않는다.
   고칠 만해 보여도 **보고만 한다.**

3. **판단이 필요하면 멈춘다.**
   어느 쪽이 맞는지 불분명하면 추측으로 정하지 않는다.
   비워두고 묻는 것이 추측으로 채우는 것보다 낫다.

4. **새 설정을 만들지 않는다.**
   명칭·인물·사건·규칙을 새로 만드는 것은 별도 지시가 있을 때만이다.

---

## AI 에이전트 독립성 원칙

- **기존 보고서 의존 금지:** 다른 AI나 작업자가 남긴 요약 문서,
  검토 보고서(`drafts/reviews/` 등)를 바탕으로 답변을 생성하지 않는다.
- **독립적 분석과 판단:** 지시를 받으면 항상 요청받은 대상 원본 파일을
  스크립트나 도구로 직접 확인하고 독립적으로 교차 검증하여 답한다.
- **숫자는 직접 센다.** 지시서에 적힌 수치도 그대로 믿지 않는다.
  다르게 나오면 보고한다.

---

## 세계관 금칙 — 새 문장을 쓸 때만 해당

파일 이동·이름 변경·숫자 정정에는 해당하지 않는다.
**본문을 새로 쓰거나 고쳐 쓸 때** 지킨다.

- **기(氣)를 실체로 쓰지 않는다.** 명칭에 氣·眞氣·內力·罡氣를 쓰지 않는다.
  내공이 소모·회복된다고 쓰지 않는다. 소모되는 것은 몸이다
- **등급표를 쓰지 않는다.** 삼류·일류·절정·화경·현경 같은 체계는 존재하지 않는다
- **심·체 수치를 계산하지 않는다.** 더하거나 평균 내거나 임계값을 만들지 않는다

자세한 것은 `canon/body-and-mind/00_INDEX.md`와
`specs/1차작업규칙.md` §1에 있다. **필요할 때만 연다.**

---

## 검증

작업 뒤 반드시 실행한다.

```
python scripts/validate.py --all      # 전부 통과해야 한다 (실패 0)통과해야 한다
python scripts/build_index.py --check # 색인 최신 확인
```

세력 문서의 항목(명칭·한자·origin)을 건드렸다면
`python scripts/build_index.py`로 색인을 재생성한다.

---

## Git

- 커밋 메시지에 **공동저자 표기나 도구 서명을 넣지 않는다.**
- **`canon/` 변경은 별도 커밋으로 분리한다.**
- 검증을 통과한 뒤에만 커밋한다.

---

## 더 필요하면

`CLAUDE.md`에 작업 절차와 정전 라우팅이 있다.
**세력 문서를 새로 만들거나 설정을 판단하는 작업**이면 그것을 읽는다.
파일 조작·숫자 정정·검색이면 이 문서로 충분하다.
